async def classify_message(message_id, channel_id, channel_name,
                           content, image_b64, context_messages):
    print(f"   🤖 Classifier placeholder — message {message_id} received")
